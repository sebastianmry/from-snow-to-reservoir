# Full dependency set for the data pipeline (download + extract + render).
# Run locally in the conda env `georgia-sar`. The deployed dashboard does NOT
# need these - its slim runtime deps live in requirements.txt (read by
# Streamlit Cloud). Install with:  pip install -r requirements-pipeline.txt
#
# Pinned to the working georgia-sar env (mirrors requirements.txt) so the weekly
# CI build is reproducible and cannot break on an upstream release between runs.
# earthaccess / rasterio / rioxarray change their APIs often; an unpinned set
# installs the latest each run and silently drifts. Bump these deliberately.
earthaccess==0.17.0
rioxarray==0.19.0
rasterio==1.4.4
tqdm==4.67.3
python-dotenv==1.2.2
numpy==2.4.6
pandas==3.0.3
pyarrow==24.0.0
geopandas==1.1.3
shapely==2.1.2
scipy==1.17.1
requests==2.34.2
streamlit==1.58.0
plotly==6.7.0
folium==0.20.0
streamlit-folium==0.27.2
